name: CICD Timeoffss
on: [push]
jobs:
  restart-pm:
    runs-on: self-hosted
    steps:
    - name: Pull repository
      run: |
        cd /home/ubuntu/GithubAction-test
        sudo git pull origin main

